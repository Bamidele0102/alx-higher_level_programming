-- Computes the score average of all record in the table
-- Result column name should be 'average'
-- Database name will be passed as an argument of the mysql command

SELECT AVG(score) AS 'average'
FROM second_table;
